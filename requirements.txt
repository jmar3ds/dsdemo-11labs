# dsdemo-11labs, voice AI deployment prototype
# Tested with Python 3.11+

elevenlabs>=1.0.0          # Official ElevenLabs Python SDK
python-dotenv>=1.0.0       # Loads ELEVENLABS_API_KEY from .env
click>=8.1.0               # CLI argument parsing
rich>=13.7.0               # Pretty console output (graceful fallback if unavailable)

# ElevenAgents demo dependencies
httpx>=0.27,<1.0           # Async HTTP client for mocked Magalu tool calls
tenacity>=8.0,<10.0        # Retry decorator with exponential backoff
pytest>=8.0,<9.0           # Test runner for local orchestration tests under tests/
pytest-asyncio>=0.23,<1.0  # Async test support
respx>=0.21,<1.0           # HTTP transport mocking for test_retry.py
fastapi>=0.110,<1.0        # Mock Magalu backend in mock_backend/server.py
uvicorn>=0.27,<1.0         # ASGI server for the mock backend
